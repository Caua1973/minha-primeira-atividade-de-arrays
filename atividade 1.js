let nomes = ['Alice', 'Bob', 'Charlie', 'João', 'Maria'];
console.log(nomes);
console.log(nomes[0]);
console.log(nomes[4]);

let  frutas = ['Maçã', 'Banana', 'Laranja', 'Uva',];
    for (let i = 0; i < 4; i++) {
        console.log(frutas[i]);
    }
let cores = ['Vermelho', 'Azul', 'Verde', 'Amarelo', 'Roxo'];
for (let i = 0; i < 5; i += 2) {
    console.log(cores[i]);
}
let produtos = ['Notebook', 'Smartphone', 'Tablet']
produtos[1] = 'Smartwatch';
console.log(produtos);

let cidades = ['São Paulo', 'Rio de Janeiro', 'Belo Horizonte', 'Curitiba'];
console.log(cidades.length);
